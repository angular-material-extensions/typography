import { MatTextRightDirective } from './mat-text-right.directive';

describe('MatTextJustifyDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextRightDirective();
    expect(directive).toBeTruthy();
  });
});
