import { MatTextLeftDirective } from './mat-text-left.directive';

describe('MatTextJustifyDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextLeftDirective();
    expect(directive).toBeTruthy();
  });
});
