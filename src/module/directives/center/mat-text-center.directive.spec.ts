import { MatTextCenterDirective } from './mat-text-center.directive';

describe('MatTextJustifyDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextCenterDirective();
    expect(directive).toBeTruthy();
  });
});
