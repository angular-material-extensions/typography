import { MatTextColorDirective } from './mat-text-color.directive';

describe('MatTextColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextColorDirective();
    expect(directive).toBeTruthy();
  });
});
