import { MatTextLowercaseDirective } from './mat-text-lowercase.directive';

describe('MatTextJustifyDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextLowercaseDirective();
    expect(directive).toBeTruthy();
  });
});
