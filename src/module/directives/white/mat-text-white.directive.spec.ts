import { MatTextWhiteDirective } from './mat-text-white.directive';

describe('MatTextWhiteDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextWhiteDirective();
    expect(directive).toBeTruthy();
  });
});
