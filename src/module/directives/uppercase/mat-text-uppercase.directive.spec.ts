import { MatTextUppercaseDirective } from './mat-text-uppercase.directive';

describe('MatTextUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
