import { MatTextHideDirective } from './mat-text-hide.directive';

describe('MatTextHideDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextHideDirective();
    expect(directive).toBeTruthy();
  });
});
