import { MatTextCapitalizeDirective } from './mat-text-capitalize.directive';

describe('MatTextCapitalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextCapitalizeDirective();
    expect(directive).toBeTruthy();
  });
});
