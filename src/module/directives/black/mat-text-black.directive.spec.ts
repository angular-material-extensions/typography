import { MatTextBlackDirective } from './mat-text-black.directive';

describe('MatTextBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextBlackDirective();
    expect(directive).toBeTruthy();
  });
});
