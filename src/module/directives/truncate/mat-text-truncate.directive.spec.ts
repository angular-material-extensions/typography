import { MatTextJustifyDirective } from './mat-text-truncate.directive';

describe('MatTextJustifyDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextJustifyDirective();
    expect(directive).toBeTruthy();
  });
});
