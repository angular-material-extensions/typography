import { MatTextMutedDirective } from './mat-text-muted.directive';

describe('MatTextMutedDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTextMutedDirective();
    expect(directive).toBeTruthy();
  });
});
